{{ form_start(form) }}
    {{ form_widget(form) }}
        <input type="submit" value="{{ 'Delete User' }}" class="btn btn-lg btn-block btn-danger" onclick="return confirm('Desea eliminar?');" />
{{ form_end(form) }}